---
- docker:
    name: dind
    image: "docker:1.9-dind"
    command: "--storage-driver=overlay"
    state: reloaded
    restart_policy: always
    privileged: yes
    volumes:
      - "dind:/var/lib/docker"
      - "{{ gitlab_builds_volume }}"
